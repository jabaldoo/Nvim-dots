# Nvim Configuration

This repository contains my personal Neovim configuration.

## Installation

To install this configuration, clone the repository into your `~/.config/nvim` directory:

```bash
git clone https://github.com/jabaldoo/Nvim-dots.git ~/.config/nvim
```

After cloning, open Neovim. Lazy.nvim will automatically install the necessary plugins.
